<!DOCTYPE html>
<html>
    {% include 'head' %}
<body>
    {% include 'navbar' %}
    <div class="container body-content">

        <h1>{{page.title}}</h1>
        {% block content %}
        {{content}}
        {%endblock%}

        Pages:<br />
        {% for item in site.pages %}
        PAGE: {{item.title}}<br />
        {% endfor %}

        {% include 'footer' %}
    </div>
</body>
</html>
